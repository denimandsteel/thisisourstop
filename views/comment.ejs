<div id="comment-<%= comment.cid %>" class="comment<% if (typeof new_comment !== 'undefined' && new_comment === true) { %> new<% } %>">
  <div class="time"><%=: comment.time | time %></div>
  <!--<div class="score"><%=: comment.score %></div>-->
  <% if (typeof comment.type !== 'undefined') { %>
    <% for(var i = 0; i < comment.type.length; i++) { %>
      <img width="32" height="32" class="icon" id="<%= comment.type[i] %>" src="/images/<%= comment.type[i] %>.png" />
    <% } %>
  <% } %>
  <div class="text"><%= comment.comment %></div>
  <div class="actions"><a href="/stop/<%= comment.stop %>/comment/<%= comment.cid %>/flag/up?token=">+</a> / <a href="/stop/<%= comment.stop %>/comment/<%= comment.cid %>/flag/down?token=">-</a> | <a class="report" href="/stop/<%= comment.stop %>/comment/<%= comment.cid %>/flag/report?token=">report</a></div>
</div>
